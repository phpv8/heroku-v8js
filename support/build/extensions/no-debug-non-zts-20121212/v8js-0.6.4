#!/usr/bin/env bash
# Build Path: /app/.heroku/php/
# Build Deps: extensions/no-debug-non-zts-20121212/v8js-bare-0.6.4, libraries/v8

# That's it - we're combining the dep build and the extension build here

source $(dirname $0)/v8js
